'use client';
import SliderImagesControl from '@/components/management/content-manager/SliderImagesControl';
import React from 'react'

export default function SliderControlClientPage({images}: {images: string[]}) {
  return (<SliderImagesControl name='' onChange={()=>{}}/>)
}
